package concurrenthashmap;

/**
 * 可以自己写一个String类，但是会覆盖java.lang.String，导致编译错误，无法使用String类的功能。
 *
 * 如果其他类要执行main方法，需要显式指定import java.lang.String，否则会默认使用当前包下的String类，导致编译错误。
 */
public class xxxString {
}
